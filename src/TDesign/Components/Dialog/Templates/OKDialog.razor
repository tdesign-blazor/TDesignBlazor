@namespace TDesign

<TDialog>
    <HeaderContent>qwdqwdqwdqwd</HeaderContent>
    <ChildContent>
        @GetContent()
    </ChildContent>
    <FooterContent>
        <TButton Theme="Theme.Primary" OnClick="Context.Cancel">确定</TButton>
    </FooterContent>
</TDialog>

@code{
    [CascadingParameter]DialogContext Context{ get; set; }

    RenderFragment? GetContent() => Context.Parameters["Content"] as RenderFragment;
}
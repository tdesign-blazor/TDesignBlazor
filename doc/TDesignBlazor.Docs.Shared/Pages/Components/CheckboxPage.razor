@page "/components/checkbox"
<PageHeader Title="Checkbox 多选框">
    多选框是一个选择控件，允许用户通过单击在选中和未选中之间切换。
</PageHeader>
<HighlightAlert/>
<Example Title="基本使用">
    <Description>最简单的多选框形式，常用于表单中数据多选的场景。</Description>
    <RunContent>
        <CheckBox @bind-Value="DemoChecked">选择我</CheckBox>
        <CheckBox @bind-Value="DemoChecked" Disabled>禁用选择</CheckBox>
        <CheckBox @bind-Value="IndeterminateCheck" Indeterminate>半选择</CheckBox>
    </RunContent>
    <CodeContent>
        @Code.Create(@"
```html
<CheckBox bind-Value=""DemoChecked"">选择我</CheckBox>
<CheckBox @bind-Value=""DemoChecked"" Disabled>禁用选择</CheckBox>
```
```cs
@code{
    bool? DemoChecked{ get; set; }
}
```
")
    </CodeContent>
</Example>
@code{
    bool? DemoChecked{ get; set; }
    bool? IndeterminateCheck{ get; set; }
}